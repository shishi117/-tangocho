# 単語帳ツール 要件定義書

- 版数: v1.1
- 作成日: 2026-08-14
- 対象読者: エンジニア（設計・実装に使用）
- 記述方針: 仕様書表現（「〜とする」「〜であること」）。曖昧表現を避け、数値基準を明示する。

### 変更履歴
| 版 | 日付 | 変更概要 |
|---|---|---|
| v1.0 | 2026-08-14 | 初版 |
| v1.1 | 2026-08-14 | 品質チェック18件を反映。用語統一（単語帳）、非機能の数値化（同期性能・保守性・アクセシビリティ・RPO/RTO）、FR005/FR009/FR012/FR014/FR016/FR017の詳細化、テスト要件（第10章）とセキュリティルール（付録A）を追加。 |

---

## 1. システム概要

本システムは、個人の資格試験学習を目的とした単語帳（フラッシュカード）Webアプリケーションとする。利用者は単一（開発者本人）とし、PC・スマートフォン・タブレットの複数端末から同一データにアクセスできることとする。学習データはクラウド（Cloud Firestore）に保存し、端末間で同期されること。

段階リリースを採る。MVP（Minimum Viable Product）は本書の必須要件（M）を満たすものとし、拡張要件（E）は後続フェーズで実装する。MVPは1週間以内の稼働を目標とする。

## 2. 用語定義

| 用語 | 定義 |
|---|---|
| テーマ | 学習領域の最上位区分（例: 特定資格）。1テーマは複数の単語帳を持つ。 |
| 単語帳 | カードの集合。1単語帳は1テーマに属する。別称「デッキ」（本書では「単語帳」に統一する）。 |
| カード | 学習の最小単位。1カードは1単語帳にのみ属する（複数所属不可）。 |
| セッション | ひとまとまりの学習単位。既定は20枚を1セッションとする。 |
| 正答率 | カード／単語帳／テーマ単位の（正答数 ÷ 回答数）。全期間と直近を区別する。 |
| 直近 | 直近20回の回答を対象とする集計範囲とする。 |
| 習得 | 「正答率80%以上 かつ 累計回答5回以上」を満たす状態とする（閾値・最低回答数は設定変更可）。 |
| 習得率 | 単語帳またはテーマ内で習得状態にあるカードの割合とする。進捗表示の主指標とする。 |
| 間隔反復 | 苦手なカードを高頻度、習得済みを低頻度で出題する方式。MVPはLeitner方式を採用する。 |
| A方式 | Claude Codeで生成したカードをCSVとして取り込む生成経路（MVP）。 |
| D方式 | 生成用プロンプトを表示し、Claudeチャットの出力CSVを取り込む半自動生成経路（MVP）。 |
| B/C方式 | アプリ内ボタンによる実行時AI生成（拡張要件）。 |

## 3. 業務要件

| ID | 内容 | 測定基準 |
|---|---|---|
| BG-1 | 複数端末で資格試験の単語学習を継続できること | PC/スマホ/タブレットで同一データに同期アクセス可能であること |
| BG-2 | 習得目標をユーザーが設定できること | 習得判定の閾値（%）・最低回答数を設定変更できること。既定は80%／5回 |
| BG-3 | 日次学習の継続を支援すること | 連続学習日数を表示できること |
| BG-4 | 数年規模の運用に耐えること | 1,000カード以上（100カード×10単語帳以上）で機能・性能要件を満たすこと |
| BG-5 | 運用コストが完全無料であること | カード登録不要の無料枠（Firebase Spark等）内で稼働すること。Claudeサブスク枠内のClaude Code利用は許容 |
| BG-6 | 運用手直しが軽微であること | 定常運用の手作業が月1回程度に収まること |
| BG-7 | MVPを短期で稼働させること | 必須要件（M）を1週間以内に稼働させること |

## 4. 機能要件

各機能は〔目的／処理内容／入出力／例外処理〕を定義する。区分は M=MVP必須、E=拡張。

### FR001 カードデータモデル定義（M）
- 目的: 学習・統計・間隔反復に必要なカード属性を一意に定義する。
- 処理内容: 1カードは次の項目を持つこととする。`cardId`（自動採番）／`deckId`（所属単語帳）／`front`（表面）／`back`（裏面）／`term`（語）／`meaning`（意味）／`example`（例文）／`explanation`（解説）／`partOfSpeech`（品詞）／`tags`（文字列配列）／`importance`（重要度: 整数1〜5、既定3）／`answerCount`（回答数）／`correctCount`（正答数）／`accuracy`（正答率、自動算出）／`box`（Leitner箱番号1〜5、既定1）／`isDeleted`（論理削除フラグ）／`owner`（所有者UID）／`createdAt`／`updatedAt`。
- 入出力: 入力＝上記項目。出力＝Firestoreドキュメント。`accuracy`は`correctCount ÷ answerCount`で算出し、`answerCount=0`のとき0とする。
- 例外処理: `term`が空文字の場合は登録不可とする。`importance`が範囲外の場合は既定値3に補正する。

### FR002 階層構造（M）
- 目的: 学習単位を体系化する。
- 処理内容: 「テーマ ＞ 単語帳 ＞ カード」の3階層とする。1カードは1単語帳（＝1テーマ）にのみ所属し、複数所属は不可とする。
- 入出力: 入力＝テーマ名・単語帳名。出力＝階層構造データ。
- 例外処理: 同一テーマ配下で単語帳名が重複する場合は登録不可とし、警告を表示する。

### FR003 生成ソース抽象化（M）
- 目的: 生成方式を差し替え可能にし、将来のB/C追加を低コストにする。
- 処理内容: カード登録の入口を「CSV取り込み（FR005）」に一本化する共通インターフェースを設ける。インターフェース契約（入出力スキーマ）は「入力＝正規化済みカード配列（FR001準拠）」「出力＝{成功件数, スキップ件数, エラー件数, エラー行番号配列}」と定義する。MVPはA方式・D方式が本入口を利用する。B/C方式は同インターフェースへ書き込む拡張として追加する。
- 入出力: 上記契約に従う。
- 例外処理: 未対応の生成ソースが指定された場合はエラーを返し、登録処理を実行しないこと。

### FR004 生成プロンプト提示（D方式）（M）
- 目的: Claudeサブスク（チャット）を用いた無料生成を支援する。
- 処理内容: ユーザーがテーマ名・資格名を入力すると、「10枚分を〔語・意味・例文・解説・品詞〕付きでCSV形式（FR005の列定義に準拠）で出力せよ」という指示文を生成し表示する。ユーザーはこれをClaudeチャットへ貼付し、得られたCSVをFR005で取り込む。
- 入出力: 入力＝テーマ名・資格名（各1〜100文字）。出力＝プロンプト文字列。
- 例外処理: 入力が空の場合はプロンプトを生成せず、入力を促すメッセージを表示する。

### FR005 CSV一括取り込み（M）
- 目的: 生成結果および既存リストを一括登録する。
- 処理内容: CSVを読み込み、FR001のカードへ正規化して登録する。列定義は `term,meaning,example,explanation,partOfSpeech,tags,importance` とし、`term`のみ必須、他は空欄可とする。`tags`は`;`区切りとする。重複判定キーは（対象単語帳, `term`）の組とし、重複時の動作は取り込み時にユーザーが「スキップ／上書き／両方登録」から選択すること。
- バリデーション基準:
  - 各項目の最大長は256文字とし、超過行はエラー行として登録しない。
  - 許容文字種はUTF-8とし、制御文字（改行・タブを除く）を含む値は当該文字を除去して取り込む。
  - 列不足時は不足項目を空欄として扱う（`term`列が無い場合はファイル全体をエラーとする）。列超過時は余剰列を無視する。
- 入出力: 入力＝CSVファイル（UTF-8）＋対象単語帳＋重複時動作。出力＝登録件数・スキップ件数・エラー件数・エラー行番号。
- 例外処理: `term`が空の行はエラー行として登録せず、行番号を報告する。`importance`非数値・範囲外は既定3へ補正する。1ファイル上限は10,000行とし、超過時は取り込みを中止し警告する。

### FR006 取り込み前プレビュー・編集（M）
- 目的: 誤登録を防ぐ。
- 処理内容: 取り込み対象を確定登録前に一覧表示し、行単位の編集・除外を可能とする。追加先単語帳を選択、または新規作成できること。
- 入出力: 入力＝正規化済みカード配列。出力＝ユーザー確定後のカード配列。
- 例外処理: 確定時に必須項目（`term`）欠落行が残る場合は、当該行を除外したうえで登録を継続し、除外件数を報告する。

### FR007 手動編集（M）
- 目的: 個別のカード保守を可能にする。
- 処理内容: カードの追加・修正・削除を個別に実行できること。削除は論理削除（`isDeleted`フラグ）とし、物理削除はMVP対象外とする。論理削除カードは無期限保持する。
- 入出力: 入力＝編集内容。出力＝更新後カード。
- 例外処理: 存在しない`cardId`への操作はエラーを返す。

### FR008 出題（M）
- 目的: 学習を実施する。
- 処理内容: カードめくり式で出題する。表面を表示し、ユーザー操作で裏面を表示、ユーザーが「正解／不正解」を自己申告する。出題順は間隔反復（FR010）に従う。
- 入出力: 入力＝対象単語帳／セッション設定。出力＝出題カード列・自己申告結果。
- 例外処理: 対象単語帳に有効カード（`isDeleted=false`）が0件の場合はセッションを開始せず通知する。

### FR009 成績更新（M）
- 目的: 正答率と間隔反復状態を維持する。
- 処理内容: 回答ごとに該当カードの`answerCount`を+1、正解時は`correctCount`を+1し、`accuracy`を再算出する。Leitner箱は、正解時`box`を+1（上限5）、不正解時`box`を1にリセットする。
- 同期・再送: 書き込み失敗時は最大3回まで再試行する。失敗が続く場合、または無料枠超過時（FR017）は、結果をローカルに保持する。再送契機は「オンライン復帰時」および「次回起動時」とする。ローカル保持の上限は直近500件とし、超過時は最古から破棄して警告する。最終的な同期失敗時はユーザーに警告表示する。
- 入出力: 入力＝自己申告結果。出力＝更新後カード。
- 例外処理: 上記同期・再送に従う。

### FR010 間隔反復（出現率変動）（M）
- 目的: 学習効率を高める。
- 処理内容: MVPはLeitner方式（5箱）とする。出題選択は箱番号による重み付け抽選とし、重み比を `box1:box2:box3:box4:box5 = 16:8:4:2:1` とする。新規カードは`box1`から開始する。習得判定（用語定義）は統計表示用であり、抽選には`box`を用いる。将来、SM-2等へ差し替え可能な構造とすること。
- 入出力: 入力＝対象カード集合・各カードの`box`。出力＝出題順（重み付き抽選結果）。
- 例外処理: 全カードが同一箱の場合は一様抽選にフォールバックする。

### FR011 セッション（M）
- 目的: 学習を区切る。
- 処理内容: 1セッションは既定20枚とし、設定で「単語帳一巡」も選択可能とする。セッション終了時に当該セッションの正答率・枚数を表示する。
- 入出力: 入力＝セッション設定。出力＝セッション結果サマリ。
- 例外処理: 対象カードが設定枚数未満の場合は存在枚数で終了する。

### FR012 テーマ・単語帳切替（M）
- 目的: 学習対象を随時変更する。
- 処理内容: 画面構成はリスト形式とし、テーマをプルダウンで選択、その配下に紐づく単語帳をリスト表示する。単語帳を選択すると学習対象が確定する。学習途中でもテーマ・単語帳を切り替え可能とし、切替時、進行中セッションは中断として扱い成績を確定保存する。
- 入出力: 入力＝テーマ選択（プルダウン）／単語帳選択（リスト）。出力＝切替後の学習画面。
- 例外処理: 切替先が存在しない場合は切替を行わず通知する。

### FR013 正答率集計（M）
- 目的: 学習状況を定量把握する。
- 処理内容: 正答率をカード／単語帳／テーマの各単位で算出し、全期間と直近（直近20回）を区別して表示する。
- 入出力: 入力＝集計単位・範囲。出力＝正答率値。
- 例外処理: 回答実績が無い単位は「未計測」と表示する。

### FR014 進捗表示（M）
- 目的: 継続を可視化する。
- 処理内容: 進捗の主指標は「習得率」とする。加えて連続学習日数、テーマ別の習得率を表示する。連続学習日数は「1日1回以上のセッション完了」を継続条件とする。習得率の算出には習得判定（正答率80%以上かつ累計5回以上）を用いる。
- 入出力: 入力＝学習履歴。出力＝進捗指標（習得率・連続日数）。
- 例外処理: 履歴欠損時は当該指標を0または「未計測」とする。

### FR015 検索・絞り込み・並び替え（M）
- 目的: カード管理を容易にする。
- 処理内容: 単語帳内で`term`・`tags`による検索、`box`・`accuracy`・`importance`による絞り込み・並び替えを可能とする。
- 入出力: 入力＝検索条件。出力＝該当カード一覧。
- 例外処理: 該当0件時は空一覧と「該当なし」を表示する。

### FR016 認証・アクセス制御（M）
- 目的: 本人のみ利用可能とする。
- 処理内容: Firebase AuthenticationのGoogleサインインを用いる。単一利用者前提のため、許可はFirestoreセキュリティルールに自分のUIDを直書きする方式（許可リスト）とし、更新はルール再デプロイで行う。Firestoreセキュリティルールで、許可UIDかつ所有者一致のデータのみ読み書きできることを保証する（付録A）。
- トークン失効: 認証トークン失効（セッション切れ）を検知した場合、進行中セッションの成績をローカル保全し、再認証後に反映する。
- 入出力: 入力＝Googleアカウント。出力＝認証可否。
- 例外処理: 許可リスト外アカウントはサインイン後にアクセス拒否とし、データへ到達させない。

### FR017 無料枠・失敗時の挙動（M）
- 目的: 無料枠逸脱とデータ不整合を防ぐ。
- 処理内容: Firestore無料枠の逸脱兆候（読み書きエラー）およびCSV取り込み・成績同期の失敗を検知し、エラーメッセージとして警告表示する。
- 無料枠超過時の状態遷移: 「学習：可」「成績記録：可（FR009に従いローカル保持、枠回復後に同期）」「新規生成・CSV取り込み：不可」とする。すなわち、追加書き込みを伴う生成系操作を停止し、学習継続と成績のローカル記録は維持する。
- 入出力: 入力＝各処理の結果。出力＝警告表示・状態遷移。
- 例外処理: 枠回復後、ローカル保持済み成績を自動同期する。

### FR018 データエクスポート（E）
- 目的: 任意のバックアップ・持ち出し手段を提供する。RPO要件（第5章）の充足手段とする。
- 処理内容: 単語帳またはテーマ単位でカードをCSV出力する。論理削除済みカードの出力要否は設計で確定する。
- 入出力: 入力＝出力対象。出力＝CSVファイル。
- 例外処理: 出力対象0件時はファイルを生成せず通知する。

### FR019 オフライン学習（E）
- 目的: 電波非依存の学習を可能にする。
- 処理内容: オフライン時に学習・成績記録を可能とし、オンライン復帰時に同期する。競合時の解決方針は既定で「最終更新優先」とし、詳細は設計で確定する。
- 入出力: 入力＝オフライン操作。出力＝同期結果。
- 例外処理: 同期競合時は既定で「最終更新優先」とする。

### FR020 アプリ内AI生成（B/C方式）（E）
- 目的: アプリ内ボタンによる実行時生成を提供する。
- 処理内容: サーバー側中継処理（Cloudflare Worker）を経由してAI APIを呼び出し、生成結果をFR003の共通入口へ渡す。APIキーはWorker側で保持し、ブラウザに露出しないこと。無料維持のため中継はカード登録不要のCloudflare Workerを用いる。
- 認可: WorkerはリクエストのFirebase IDトークンを検証し、許可UIDのリクエストのみAI呼び出しを許可すること。
- 入出力: 入力＝テーマ・生成枚数。出力＝生成カード配列。
- 例外処理: API失敗・枠超過時はエラー警告を表示し、A/D方式へ縮退できること。

## 5. 非機能要件

| 区分 | 要件 | 測定基準 |
|---|---|---|
| 性能 | カード表示応答 | カードめくりの表示切替は0.2秒以内であること |
| 性能 | CSV取り込み | 1,000行の取り込み処理を10秒以内で完了すること（AI生成応答時間は本システム外のため性能基準から除外） |
| 性能 | 端末間同期 | オンライン時、1件更新の他端末反映を95パーセンタイルで5秒以内とすること |
| 可用性 | 無料枠特性の許容 | コールドスタート（休眠後の初回起動遅延）・一時停止を許容する |
| 可用性 | RPO/RTO | RPOは1週間（週次のFR018エクスポート運用を前提）とする。RTOは個人用のためベストエフォート（無保証）とする。論理削除カードは無期限保持する |
| セキュリティ（認証） | 本人限定 | 許可リスト（ルール直書きUID）内のアカウントのみアクセス可能であること |
| セキュリティ（データ分離） | ルール強制 | Firestoreセキュリティルール（付録A）で未認証・許可外・非所有データへのアクセスを100%遮断すること |
| セキュリティ（鍵） | 鍵非露出 | MVPはアプリからAI APIを呼ばず隠匿対象の鍵を持たない。B/C導入時は鍵をWorker側にのみ保持すること |
| コスト | 完全無料 | カード登録不要の無料枠内で稼働すること。Firestore日次上限（読取50,000・書込20,000等）を超えない設計とすること |
| 保守性 | テスト | 主要ロジック（間隔反復抽選・正答率算出・CSV正規化）の行カバレッジを80%以上とすること |
| 保守性 | コード規約 | Lint（ESLint）と整形（Prettier）の既定ルールを適用すること。生成ソースIFの入出力スキーマ（FR003）を維持すること |
| アクセシビリティ | 可読性・操作性 | WCAG 2.1 AA相当を目安とし、本文フォント最小16px、コントラスト比4.5:1以上、主要操作をキーボードで実行可能とすること |
| 対応環境 | ブラウザ | Google Chrome・Safari（PC・スマホ・タブレット）最新安定版で動作すること |
| 監視 | 異常検知 | 無料枠超過・処理失敗を検知し警告表示すること |
| プライバシー | 機微情報非送信 | カード内容に外部AIへ送信不可の機微情報を含めない運用とすること |
| 言語 | 日本語 | UI・カード内容は日本語を基本とすること |

## 6. 構成・外部連携

- ホスティング: Firebase Hosting（Spark、カード不要）
- データストア: Cloud Firestore（Spark、日次無料枠内）
- 認証: Firebase Authentication（Googleサインイン＋ルール直書き許可リスト）
- ソース管理: GitHub（Private リポジトリ）
- 生成（MVP）: A方式＝Claude CodeでCSV生成→GitHubコミットまたはローカル保存→FR005で取り込み（サービスアカウント鍵不要）。D方式＝FR004のプロンプト→Claudeチャット→FR005で取り込み。
- 生成（拡張）: B/C方式＝Cloudflare Worker経由のAI API呼び出し（FR020）。Firebase Functionsは無料枠でカード登録が必要なため採用しない。
- 開発体制: 単独（PM兼開発）＋Claude Code（AIDD）。

## 7. 制約条件

| ID | 制約 |
|---|---|
| C-1 | 運用は完全無料（カード登録不要の無料枠内）であること |
| C-2 | データストアはCloud Firestoreとする |
| C-3 | 生成品質を優先し、MVPはClaude（サブスク枠のClaude Code）を用いる |
| C-4 | 1カードは1単語帳（1テーマ）にのみ所属する |
| C-5 | 対応端末は複数（PC・スマホ・タブレット）。利用者は単一 |
| C-6 | UI言語は日本語 |

## 8. テスト要件

| TR | 対象 | 検証内容・合否基準 |
|---|---|---|
| TR-1 | FR010 間隔反復 | 大量試行（例10万回）で箱別抽選比率が16:8:4:2:1に±5%で収束すること |
| TR-2 | FR005 取り込み性能 | 1,000行取り込みが10秒以内で完了すること |
| TR-3 | FR005 バリデーション | 256文字超過行・`term`空行・列不足/超過の各ケースで規定どおり除外/補正されること |
| TR-4 | BG-1 端末間同期 | 2端末での同時更新シナリオで、最終更新優先の期待結果に収束すること |
| TR-5 | FR017 無料枠超過 | エミュレータまたは擬似エラー注入で超過状態を再現し、「学習可・成績ローカル保持・生成不可」と警告表示を確認すること |
| TR-6 | FR016 認可 | 許可外UIDおよび未認証アクセスがセキュリティルールで遮断されること |
| TR-7 | FR009 再送 | 書き込み失敗→ローカル保持→オンライン復帰時同期の一連が成立すること |

## 9. 残未確認事項（設計フェーズで確定）

| ID | 内容 |
|---|---|
| U-2 | 将来の他者共有可否（現状MVPは自分専用で確定） |
| U-3 | 間隔反復の最終方式（MVPはLeitner。SM-2等への差し替え要否） |
| U-10 | 想定読み書き回数のFirestore無料枠試算 |
| E-2 | FR019のオフライン競合解決の詳細（既定は最終更新優先） |
| E-3 | FR018の論理削除済みカードのエクスポート要否 |

## 10. 拡張要件一覧（後続フェーズ）

- FR018 データエクスポート
- FR019 オフライン学習
- FR020 アプリ内AI生成（B/C方式、Cloudflare Worker中継）

## 付録A Firestoreセキュリティルール（疑似コード）

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 許可UIDを直書き（単一利用者前提）
    function isAllowed() {
      return request.auth != null
        && request.auth.uid in ['<OWNER_UID>'];
    }
    function isOwner(doc) {
      return isAllowed() && doc.owner == request.auth.uid;
    }
    match /cards/{cardId} {
      allow read: if isOwner(resource.data);
      allow create: if isAllowed() && request.resource.data.owner == request.auth.uid;
      allow update, delete: if isOwner(resource.data);
    }
    match /decks/{deckId} {
      allow read: if isOwner(resource.data);
      allow write: if isAllowed() && request.resource.data.owner == request.auth.uid;
    }
    match /{document=**} {
      allow read, write: if false; // 既定は全遮断
    }
  }
}
```
